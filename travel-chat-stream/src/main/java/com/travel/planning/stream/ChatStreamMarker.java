package com.travel.planning.stream;

/**
 * travel-chat-stream 模块包锚点（M16-4；本包为该模块独占）。
 *
 * <p>仅作 {@code @ComponentScan(basePackageClasses=...)} 的编译期锚点：包重命名/
 * 迁移时此处编译失败而非 Bean 静默丢失（取代字符串 scanBasePackages）。无任何成员。</p>
 */
public interface ChatStreamMarker {
}
